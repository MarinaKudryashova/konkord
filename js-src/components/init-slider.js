import Swiper from "swiper";
import { Navigation, EffectFade, FreeMode, A11y, Thumbs } from "swiper/modules";
Swiper.use([Navigation, EffectFade, FreeMode, A11y, Thumbs]);

export { Swiper };
export default Swiper;
